apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: deployment
  namespace: default
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: deployment
    spec:
      containers:
      - name: deployment
        image: kubelabs/tensorflow
